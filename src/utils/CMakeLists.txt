# =============================================================================
# utils – Header-only (no sources to collect, just include path)
# =============================================================================
set(UTILS_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/include PARENT_SCOPE)
